import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  scrollContainer: {
    marginTop: 18,
    paddingBottom: 20,
    display: "flex",
    rowGap: 16,
  },
});
export default styles;